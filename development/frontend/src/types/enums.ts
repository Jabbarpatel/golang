export enum Roles {
  ADMIN = "Admin",
  USER = "User",
}

export enum NotificationTypes {
  INFO = "info",
  SUCCESS = "success",
  ERROR = "error",
  WARNING = "warning",
}

export enum Actions {
  UPDATE = "update",
  CREATE = "create",
  DELETE = "delete",
}
